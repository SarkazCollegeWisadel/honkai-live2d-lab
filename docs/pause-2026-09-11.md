# 暂停记录 · 2026-09-11

用户在原定 12:50 前要求提前暂停，已停止新增实现和测试，保存现有成果。

## 已完成

- P2G 原生接口导入、保存、渲染 Haru 样例；云端音频驱动口型及保持表情通过采样验证。
- AIRI 安装、样例导入和 Step 云端 TTS 请求通过；本地 TTS 已停止，不自动重启。
- 原创六表情桥接代码与三个逻辑测试已保存。
- 两版全身立绘草案已留在本地 `art/luna/drafts/`。尚未审定，均为 RGB 图片，棋盘格是背景像素，不能当透明素材或完成的 Live2D。

## 尚未完成

- AIRI 实际对话返回了问候，但没有执行表情工具；默认角色提示词诱导模型使用文本 CALL 标记，且推理过长。直接 Step API 的工具调用已成功，不能据此声称 AIRI 整条链路成功。
- 角色与配置的完整重启验证、P2G 150% 缩放位置问题。
- 正式立绘审阅、真透明拆层、PSD、Cubism 可编辑工程、动作物理、面捕、换装与小格蕾修。

## 恢复顺序

1. 为项目创建独立 AIRI 角色配置，禁用不必要的长推理，验证真实工具执行及云端语音联动。
2. 审阅默认服饰草案的脸、服装与站姿；修复背景并验证可用拆层流程，再进入 Cubism 绑定。
3. 完成 P2G 重启和缩放验收，再实现可重复安装流程。

凭证和调试记录仅保存在被 Git 忽略的本地目录。正式模型尚不可下载使用；公共仓库目前提供文档、准备脚本和实验性适配代码。
