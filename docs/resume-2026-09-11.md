# 恢复后的实测 · 2026-09-11 下午

## 对话和语音

- 用户提供临时 DeepSeek key，已验证 `/models` 可用；选择 `deepseek-flash`，通过 AIRI 原生 DeepSeek provider 的 `thinkingMode: disable` 配置关闭长推理。模型名称与开关参考 [官方模型说明](https://api-docs.deepseek.com/quick_start/pricing/) 和 [Thinking Mode](https://api-docs.deepseek.com/guides/thinking_mode/)。
- 新建独立“互动验证”角色卡，保存文本服务、Step 通用测试音色和 Haru 样例模型；默认卡保留。
- 实际聊天要求开心四秒：收到真实 `honkai_set_expression` 执行结果，参数为 happy、4000 ms；三段 TTS 请求均成功，耗时约 1.62、1.67、2.43 秒。播放状态为真，口型峰值约 0.700，结束时恢复 normal。
- 第二轮“左眼眨眼＋欢迎回来”再次执行 wink 工具，表情调用成功。
- 播放中停止验证：观察到口型后停止，700 ms 后 speaking=false、mouth=0、normal；再观察 1.2 秒未恢复播放。
- AIRI 完整进程重启后，专用角色卡、DeepSeek、Step 音色、Haru 模型和会话均恢复。连接脚本重新安装桥接成功；运行时代码尚不是持久插件。

这是样例技术验证，回复措辞还会提及“眨眼完成”等动作说明，后续需调整正式角色对白。表情的计时与 TTS 排队时延尚未统一。

## P2G

- 经 Steam 启动后的新进程再次读取到已保存的 Haru Live2D 测试角色，并通过原生组件重新渲染。
- 150% 缩放下定位问题确认来自物理像素与逻辑像素混用。预览容器逻辑尺寸 1098×562，物理尺寸 1647×843。
- 预览中修正后坐标由 (823.5, 809.28) 改为 (549, 539.52)，全身居中显示。可复用计算见 `adapters/p2g/layout.mjs`；数学测试覆盖 100%、150%、200% 及重复布局。
- 该修正目前用于实验预览，没有修改游戏安装包，也尚未覆盖游戏内所有布局与窗口尺寸变化。

## 美术

背景移除后的 v03 已保存在本地，确认 PNG 为 RGBA，背景采样 alpha=0。透明草图仍需美术审阅；尚未制作 PSD、cmo3、正式表情或物理绑定。

## 后续

优先落实正式立绘拆层与可编辑绑定，再将语义参数映射到实际角色；同步完成可重复安装方式、播放对齐和换装结构。小格蕾修复用制作规范与驱动接口，美术与绑定独立制作。
